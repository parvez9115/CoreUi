import { RoleMenuFilterPipe } from './role-menu-filter.pipe';

describe('RoleMenuFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleMenuFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
